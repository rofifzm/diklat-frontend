import { reactive } from "vue";

export const cuacaStore = reactive({
  data: null,
});

// file store.js ini adalah mekanisme data management pada Vue yang dapat membuat setiap komponen dapat mengakses data yang sama